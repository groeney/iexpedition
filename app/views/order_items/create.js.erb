success_message('Successfully added <%= @product.name %> to your cart');
$('.cart-overview').html("<%= j render partial: 'includes/checkout/cart_items', locals: { order_items: @order.order_items } %>");
$('#order_items_count').text('<%= @order.order_items.count %>');
$('#price').text('<%= @order.decorate.total_or_with_discount %>');